git command=>
1.git clone 
2.git add .
3.git commit -m "just added a file"
4.git pull origin main
5.git push origin main


Git live
1.git branch 
2.live folder first before your code added git pull
3.demo code ng build --c live cut 
4.alreay live build_old delete current build 
Rename build_old finally live build add
5.git add .
6.git commit -m "update"
7.git pull
8.git push 

git working flow
----------------
1.git if your edit commit and pull (or) not edit directly pull
2.if any automerge accept current change or incoming change select again git add git commit pull and push


124 last so empty array return length calculate 0  
empty array base button hide [hidden]=isstatus==0 


backend get data true send
not data false 

<div ng-if="$ctrl.length == 0" ? true : false></div>
"0" == false 



